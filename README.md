# Gateway
Gateway es simplemente un archivo de Docker Compose que contiene la configuracion
actual para el proxy de reverso utilizado en las maquinas virtuales.

Si se agrega un nuevo servicio u endpoint se debe agregar aqui.